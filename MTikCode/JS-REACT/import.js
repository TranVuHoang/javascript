// import - export trong react
// impport named: import { Page } from "./Page.jsx";
// import default: import Page from "./Page.jsx";

// export:
// export default: vd: export default Page;
// export name -> vd: export { Page };

// khi nào dùng default export, khi nào dùng named export?
// -> Nếu trong file chỉ export đúng 1 thứ duy nhất -> dùng default export là chuẩn
// -> Nếu export nhiều thứ(nhiều hàm(component) trong cùng 1 file -> dùng named export:
