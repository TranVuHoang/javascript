Javascript DOM

- DOM:

```
Document
Object
Model
```

<pre>
  => Mô hình đối tượng tài liệu:

  Document(Tài liệu)   -> trang HTML
  Object (Đối tượng)   -> đối tượng trong JS
  Model (Mô hình)      -> cấu trúc dạng cây thể hiện quan hệ giữa các phần tử
</pre>
