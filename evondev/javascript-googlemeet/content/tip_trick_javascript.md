`1. Comment nhanh trong js:`

- Phím tắt:

```
cmd/window + /
```

`2. Tạo console.log nhanh cho 1 biến:`

- phải để con trỏ chuột vào biến trước
- sau đó dùng phím tắt:

```
ctrl + option/alt + L
```

ví dụ:

```
var course = "javascript";
console.log("console.remove - course:", course);
```
