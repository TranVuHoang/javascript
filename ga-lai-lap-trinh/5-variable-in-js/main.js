// Hàm hiển thị hộp thoại lên trên trình duyệt cho người dùng
alert("xin chào");

// Hàm hiển thị hộp nhập liệu cho người dùng
// prompt("Mời nhập vào tên của bạn:");

/**
 * Ghi chú nhiều dòng
 * dòng 1
 * dòng 2
 */

// Khai báo biến
var myName;
let yourName;
const ageUser = 29;

// Khởi tạo biến
var mathPoint = 8.25;
const physicalPoint = 8.25;
let firstName = "Hoàng";

// Xuất giá trị của biến
console.log("Điểm toán:" + mathPoint);
console.log(firstName);

// Thay đổi giá trị của biến
mathPoint = 5;
console.log("Điểm toán của bạn là: " + mathPoint + " điểm ");
console.log(firstName);

// Viết chương trình hiện lên thông báo trên màn hình: cho phép người dùng nhập tên và xuất tên trên consolog

var name = prompt("vui lòng nhập vào tên của bạn: ");
console.log("chào mừng " + name + " đến với trang tuhoc.cc");
